{
    "version": "0.2.0",
    "configurations": [
      {
        "name": "Python: Test Battle",
        "type": "python",
        "request": "launch",
        "program": "${workspaceFolder}/test/test_hp_logger_observer_battle.py",
        "console": "integratedTerminal",
        "envFile": "${workspaceFolder}/.env",
        "python": "${workspaceFolder}/.venv/bin/python"  // ← ここを追加 or 修正
      }
    ]
  }
  